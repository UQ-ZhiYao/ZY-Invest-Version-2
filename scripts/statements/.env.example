# Copy to .env and fill in. Never commit the real .env — it holds the service
# role key, which bypasses every Row Level Security policy in the project.
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJ...service-role-key-from-supabase-dashboard-settings-api
